// Rodrigo Cesar Mendes dos Santos
// Turma : M2

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main (){
    
    char palavra[100], arm[100];
    int tamanhop;

    printf("Digite a string: \n");
    gets(palavra);

    tamanhop = strlen(palavra);
    
    for(int i=0; i<tamanhop; i++){
        if(palavra[i] == 'A' || 'a' || 'B' || 'b'){ // if não esta funcionando como eu quero
            printf("%c", palavra[i]); // Esta funcionando lendo a palavra
        };
        
    };

    // O programa está lendo qualquer palavra, falta botar as restrições para as strings específicadas.
    

    
}
